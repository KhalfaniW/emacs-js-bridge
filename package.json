{
  "name": "emacs-js-bridge",
  "version": "1.0.0",
  "description": "A Node.js bridge to interact with Emacs server",
  "main": "dist/index.js",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "npm run build && bun examples/basic.js",
    "start": "npm run build && bun examples/basic.js --log-level warn",
    "start:debug": "npm run build && bun examples/basic.js --log-level debug"
  },
  "keywords": [
    "emacs",
    "nodejs",
    "bridge",
    "tcp"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "esbuild": "^0.19.5",
    "winston": "^3.8.2"
  }
}
